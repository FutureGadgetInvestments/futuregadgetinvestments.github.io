backend:
  name: git-gateway
  branch: main # Branch to update (master by default)

media_folder: 'assets/uploads'
collections:
  - name: 'blog'
    label: 'Blog'
    folder: '_posts/'
    fields:
      - { name: Title }